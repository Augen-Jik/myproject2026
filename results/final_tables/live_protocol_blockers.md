# Live Protocol Blockers

- Current scene-profile-fixed live runner coverage: `Dijkstra`, `Rule-A*`, `DQN`, `GCN-Weight`, `Sparse-LoRA`, `Sparse-LoRA+GAT`.
- PPO still lacks a same-protocol live rerun path in this chain; the available PPO row remains the direct-read quick baseline summary under `results/ppo_baseline/ppo_quick_summary.csv`.
- Therefore, the PPO row must stay explicitly marked as `quick baseline, untuned` and must not be promoted to a fully comparable live main-table result without a compatible runner.
- Legacy prompt / LoRA rows such as `Raw-Qwen`, `CoT-Qwen`, `R1-Raw`, `Qwen-LoRA`, and `R1-LoRA` are not generated by this live scene-profile runner, so rebuilding an all-method paper table on one unified live protocol is blocked in the current code path.
- Lowest viable alternative in this batch: keep `results/final_tables/method_comparison_v2.csv` as the protocol-clean main evidence table for the supported methods, and keep PPO as a separate coverage-only note.
