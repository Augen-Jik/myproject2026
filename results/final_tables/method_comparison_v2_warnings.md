# Method Comparison Warnings

- Checked duplicate scene-level signatures across `travel_time_s`, `planning_time_s`, `constraint_rate`, `signal_ratio`, `path_len`, `path_cost`, `start_edge`, `end_edge`.

- No method pair is fully identical across every checked scene.
